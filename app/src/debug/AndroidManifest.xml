<?xml version="1.0" encoding="utf-8"?>
<!-- Debug overlay: attaches the cleartext-to-localhost network security config. Merged into the
     main manifest for the debug variant only; release is untouched (cleartext stays blocked). -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application android:networkSecurityConfig="@xml/network_security_config" />
</manifest>
